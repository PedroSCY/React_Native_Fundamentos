import { StyleSheet } from "react-native";

const utils = StyleSheet.create({
    textXl: {
        fontSize: 24,
    },
    textBold: {
        fontWeight: "bold",
    },
    textRed500: {
        color: "#f44336",
    },
})

const textXl = utils.textXl;
const textBold = utils.textBold;
const textRed500 = utils.textRed500;

export { textXl, textBold, textRed500 };